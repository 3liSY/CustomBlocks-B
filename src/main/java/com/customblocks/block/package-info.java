/**
 * Minecraft Block subclasses. Primarily SlotBlock — the generic block that backs
 * each of the 1028 runtime-assignable slots (it holds a slot id and defers its
 * appearance and attributes to the SlotData assigned in SlotManager).
 */
package com.customblocks.block;
