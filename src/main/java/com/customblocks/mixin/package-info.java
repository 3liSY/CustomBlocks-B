/**
 * Fabric mixins. The keep-alive mixins (ClientKeepAliveMixin + ServerKeepAliveMixin)
 * extend timeout tolerance so players are not disconnected during the several
 * seconds a large resource-pack download can take. Added in Phase 5; the mixin
 * config (customblocks.mixins.json) currently declares no mixins.
 */
package com.customblocks.mixin;
