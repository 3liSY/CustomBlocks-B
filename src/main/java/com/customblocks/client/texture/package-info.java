/**
 * Client-side texture caching. TextureCache stores generated textures so modded
 * clients avoid redundant regeneration when the same texture is reused.
 */
package com.customblocks.client.texture;
