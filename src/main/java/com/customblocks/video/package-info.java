/**
 * Video-to-texture pipeline. VideoDecoder uses jcodec (pure-Java) to decode
 * MP4/H264 files into texture frame sequences for animated blocks. Added in
 * Phase 13.
 */
package com.customblocks.video;
