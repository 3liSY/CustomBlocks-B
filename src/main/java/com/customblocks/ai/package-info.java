/**
 * Opt-in AI features (replaces the old project's mis-named "assistant" package).
 *   • AiCommandParser    — parses natural-language input into valid /cb commands
 *   • AiTextureGenerator — generates block textures from text (requires an API key)
 *
 * All AI features are opt-in and fail with a clear message when no key is set.
 */
package com.customblocks.ai;
