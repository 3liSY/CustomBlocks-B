/**
 * Root package for CustomBlocks — a Fabric mod (Minecraft 1.21.1) that turns image
 * and GIF URLs into working, fully-attributed blocks in real time.
 *
 * Holds the mod entrypoint (CustomBlocksMod), global config (CustomBlocksConfig),
 * the block lookup utility (BlockFinder), and the mojibake repair dictionary
 * (TextSanitizer). Everything else is organised into focused sub-packages.
 *
 * Single source of truth for the architecture: CustomBlocks_Engineering_Bible.md.
 */
package com.customblocks;
