/**
 * One handler class per command group: creation, property, shape, history, utility,
 * gui, arabic, macro, and admin commands. Each handler is registered by
 * CommandRegistrar and stays under 400 lines per §9.3.
 */
package com.customblocks.command.handlers;
