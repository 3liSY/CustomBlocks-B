/**
 * One standalone class per GUI screen: main menu, block editor, texture picker,
 * shape editor, colors hub, category browser, snapshot manager, arabic browser,
 * macro manager, undo picker, import conflict, config, and admin.
 *
 * Each screen stays under 500 lines per §9.3; widgets that grow beyond that are
 * extracted into a gui/widgets package.
 */
package com.customblocks.gui.screens;
